ЗАДАЧА: Улучшить генерацию HTML для правой панели

1. ПЕРЕДАВАТЬ ТЕКУЩИЙ HTML В КОНТЕКСТ
Проблема: модель не знает что сейчас показано на панели и не может решить — менять контент или оставить.
Что сделать:

В shared/schema.ts добавить поле currentHtml: z.string().nullable().optional() в htmlRequestSchema
В client/src/pages/home.tsx передавать dynamicHtml в вызов streamHtml() и добавить его в body запроса
В server/routes.ts принять currentHtml из запроса и передать в streamOpenRouterHtml()
В промпте добавить секцию с текущим HTML (первые 500-1000 символов) чтобы модель видела что уже показано


2. ОБНОВИТЬ CSS ДЛЯ СОВРЕМЕННОГО ДИЗАЙНА
Проблема: текущий дизайн выглядит примитивно, как сайт 2005 года.
Что добавить в client/src/index.css:

Hero секции с градиентным фоном и декоративными элементами
Feature cards с hover эффектами (поднятие, тень, подсветка бордера)
Comparison table как современную сетку, не построчно
Gallery с эффектами при наведении
Process steps с вертикальной линией-коннектором между шагами
Pricing cards с выделением featured тарифа через градиент и тень
CTA кнопки с градиентом и тенью
Метрики с градиентным текстом
Анимации появления элементов (fadeInUp с задержкой для каждого элемента)
Использовать: градиенты, box-shadow, transform на hover, border с opacity от primary цвета


3. ПЕРЕДЕЛАТЬ ПРОМПТ ДЛЯ HTML ГЕНЕРАЦИИ
Проблема: контент справа дублирует чат вместо того чтобы дополнять его.
Изменить в buildHtmlSystemPrompt():
Добавить правило:
КРИТИЧЕСКОЕ ПРАВИЛО — НЕ ДУБЛИРОВАТЬ ЧАТ:
Правая панель ДОПОЛНЯЕТ разговор, а не повторяет его.

Чат говорит "10 000 резюме в день" → Панель показывает:
- Инфографику воронки: 10 000 → 500 релевантных → 50 на интервью → 5 финалистов
- Скриншот дашборда
- Кейс клиента с цифрами

Чат говорит "стоит от 8 330 рублей" → Панель показывает:
- Сравнительную таблицу с живым рекрутером
- ROI калькулятор
- Все тарифы с деталями
Добавить правило качества:
ПРАВИЛО КАЧЕСТВА:
- Генерируй HTML только если это НОВАЯ тема И можешь сделать минимум 2-3 богатые секции
- Если тема та же — верни пустую строку, текущий контент останется
- Никогда не генерируй 1 маленькую карточку — лучше оставить текущий богатый контент

4. ПОМЕНЯТЬ МОДЕЛЬ НА CLAUDE SONNET 4.5
В server/routes.ts изменить:

model: "anthropic/claude-sonnet-4" → model: "anthropic/claude-sonnet-4.5"
Это нужно сделать в обоих местах: streamOpenRouterChat и streamOpenRouterHtml